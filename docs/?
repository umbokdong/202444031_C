#include <stdio.h>
3. #pragma warning(disable:4996)
int more_big(int x, int y){
    return (x>y)? x:y;
}

int main(){
    int x,y;
    scanf("%d %d", &x, &y);
    printf("%d", more_big(x,y));
    return 0;
}
